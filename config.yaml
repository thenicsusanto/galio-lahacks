ingest:
  target_fps: 6          # frames per second to sample from each stream
  queue_maxsize: 12      # max frames buffered per stream before dropping (~2s at 6fps)

models:
  yolo: yolo26s.pt                           # path to YOLO weights file
  yolo_imgsz: 416                            # inference resolution (320/416/640)
  vlm_url: http://localhost:8000/v1/chat/completions
  vlm_model: Qwen/Qwen2.5-VL-7B-Instruct

cameras:
  - id: cam_01
    type: file
    # path: footage/Vandalism003_x264.mp4
    path: footage/Shoplifting005_x264.mp4
    loop: true

  # - id: cam_02
  #   type: file
  #   path: footage/clip2.mp4
  #   loop: true

  # Uncomment and set your phone's IP to use RTSP
  # - id: cam_rtsp
  #   type: rtsp
  #   url: rtsp://192.168.1.100:8080/h264_ulaw.sdp
  #   loop: false
